import styled from "styled-components";
import { StyledDropdown } from './common';
console.log(StyledDropdown);
export const MessagesMenu = styled(StyledDropdown)`
  --pf-dropdown-menu-min-width: 18rem;
`;